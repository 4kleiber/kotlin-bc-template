# Loads .env into the shell automatically via direnv (https://direnv.net) — run
# `direnv allow` once per clone. Keeps POSTGRES_VERSION (and anything else added to .env) in
# sync between your shell, docker-compose.yml, and Gradle without exporting it by hand.
dotenv
